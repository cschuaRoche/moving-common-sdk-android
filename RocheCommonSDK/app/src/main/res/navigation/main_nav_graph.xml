<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph">
    <!--    app:startDestination="@id/splash">


    <fragment
        android:id="@+id/splash"
        android:name="com.rochedhs.nrm.passport.patient.presentation.feature.splash.SplashFragment"
        tools:layout="@layout/f_splash">

        <action
            android:id="@+id/action_splash_to_login"
            app:destination="@id/dashboard"
            app:popUpTo="@id/splash"
            app:popUpToInclusive="true" />
    </fragment>

    <activity
        android:id="@+id/dashboard"
        android:name="com.rochedhs.nrm.passport.patient.presentation.feature.DashboardActivity"
        tools:layout="@layout/f_nav_host" />
        -->
</navigation>