<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/biometrics_nav_f"
        android:name="com.roche.roche.dis.biometrics.BiometricsFragment"
        tools:layout="@layout/biometrics_fragment" />

    <fragment
        android:id="@+id/rootedFragment"
        android:name="com.roche.roche.dis.RootedFragment"
        android:label="fragment_rooted"
        tools:layout="@layout/fragment_rooted" />

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.roche.roche.dis.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/unzipFragment"
        android:name="com.roche.roche.dis.UnzipFragment"
        android:label="fragment_unzip"
        tools:layout="@layout/fragment_unzip" />
</navigation>